# Stop quietly function Stop from a function without an error. Used for stopping when no internet

Stop quietly function Stop from a function without an error. Used for
stopping when no internet

## Usage

``` r
stop_quietly()
```
