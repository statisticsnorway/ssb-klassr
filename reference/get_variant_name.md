# Get variant name Internal function for fetching the variant name based on the number

Get variant name Internal function for fetching the variant name based
on the number

## Usage

``` r
get_variant_name(variant)
```

## Arguments

- variant:

  The variant number
